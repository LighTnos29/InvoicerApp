import React from 'react';
import { View, Text } from 'react-native';
import Register from './path/to/Register'; // Adjust the path if necessary

const App = () => {
  return (
    <Register />
  );
};

export default App;
